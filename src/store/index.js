import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex);

const state = {
}

const getters = {}

//to handle actions
const actions = {
}

const mutations = {
}

export default new Vuex.Store({
    state,
    getters,
    actions,
    mutations
})
